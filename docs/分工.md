# 分工方案

根据设计文档的目标和当前代码结构，核心思路是：**按功能模块垂直切分**，两人各自负责独立的文件和目录，最大限度避免同文件冲突。

---

## 人员 A：OS 桌面系统 + UI 层

负责所有 2D UI 组件、窗口系统、通知系统、状态面板、模拟引擎、数据层。

### 已完成任务

| 任务 | 涉及文件 / 目录 | 状态 |
|---|---|---|
| 左侧 Dock 栏（5 个 Agent 图标 + 状态灯 + 角标） | `src/components/Dock/` | ✅ 完成 |
| Agent 窗口系统（拖拽 / 缩放 / 最小化 / 单例） | `src/components/AgentWindow/` | ✅ 完成 |
| 右侧信息面板（当前代理 / 思考 / 决策链 / 事件日志） | `src/components/InfoPanel/` | ✅ 完成 |
| 底部任务栏（首页按钮 / 窗口标签 / 通知图标 / 时钟） | `src/components/Taskbar/` | ✅ 完成 |
| OS 通知系统（Toast 弹窗 / 队列 / 自动消失） | `src/components/Notification/` | ✅ 完成 |
| 快捷键帮助浮层（`?` / `F12` 触发） | `src/components/HelpOverlay/` | ✅ 完成 |
| 全局状态管理（Zustand store） | `src/stores/useScenarioStore.ts`、`useSystemStore.ts`、`useWindowStore.ts` | ✅ 完成 |
| 键盘事件监听 + `Esc` 优先级逻辑 | `src/hooks/useKeyboard.ts` | ✅ 完成 |
| 模拟引擎（4 场景 × 8 步骤） | `src/features/simulation/` | ✅ 完成 |
| 顶部 HeaderHUD | `src/components/HeaderHUD.tsx` | ✅ 完成 |
| 底部时间轴 | `src/components/BottomTimeline.tsx` | ✅ 完成 |
| 演示控制面板（基础版） | `src/components/DemoControlPanel/` | ✅ 完成 |
| DashboardPage 主页面组装 | `src/pages/DashboardPage.tsx` | ✅ 完成 |

### 待完成任务（按优先级排序）

| # | 任务 | 涉及文件 / 目录 | 说明 |
|---|---|---|---|
| 1 | 自动演示模式（Auto-Demo） | 新建 `src/features/simulation/useAutoDemo.ts` | 一键全自动走完异常→恢复流程，支持暂停/重播/循环 |
| 2 | AI 思维链内容丰富化 | 新建 `src/data/thinkingScripts.ts` | 4 个场景各 8-12 条逼真 AI 推理要点 |
| 3 | 决策链可视化增强 | 新建/改造 `src/components/InfoPanel/DecisionChain.tsx` | 连线动画、置信度、步骤高亮、预计耗时 |
| 4 | 参数渐变模拟（非跳变） | 新建 `src/features/simulation/telemetrySimulator.ts` | 3s 渐变越限 + 5s 渐变恢复 + IDLE 微波动 |
| 5 | 顶部指标实时跳动 | 改造 `src/components/HeaderHUD.tsx` | 数字滚动动画、异常变红、微小波动 |
| 6 | Agent 窗口数据卡片动画 | 新建/改造 `src/components/AgentWindow/MetricCard.tsx` | 数值平滑过渡、越限高亮、进度条可视化 |
| 7 | InfoPanel 打字机效果优化 | 改造 `src/components/InfoPanel/InfoPanel.tsx` | 逐条淡入、光标闪烁、自动滚动 |
| 8 | 底部时间轴增强 | 改造 `src/components/BottomTimeline.tsx` | 当前节点脉冲、连线渐亮、解说词显示 |
| 9 | 演示控制面板重做 | 重构 `src/components/DemoControlPanel/` | 场景选择、自动/手动、播放/暂停/速度/进度 |
| 10 | 通知→窗口联动 | 改造 `src/components/Notification/Notification.tsx` | "查看详情"按钮打开对应 Agent 窗口 |
| 11 | 快捷键改为 F1-F4 | 改造 `src/hooks/useKeyboard.ts` | F1-F4 触发场景，保留 Ctrl+1-4 兼容 |
| 12 | Esc 优先级完善 | 改造 `src/hooks/useKeyboard.ts` | 严格按规划书 5 级优先级 |
| 13 | HelpOverlay 内容更新 | 改造 `src/components/HelpOverlay/HelpOverlay.tsx` | 更新为 F1-F4 映射 |
| 14 | 场景脚本数据外置 | 新建 `src/data/scenarioScripts/` 目录 | 4 个场景完整脚本独立文件，方便编辑 |
| 15 | 演示话术/解说词 | 新建 `src/data/narratives.ts` | 每阶段解说提示，辅助演示人员 |
| 16 | Mock API 适配器模式 | 新建 `src/api/mockAdapter.ts` | 适配器架构，环境变量切换 mock/real |

---

## 人员 B：3D 场景 + 动画引擎

负责 React Three Fiber 场景、粒子系统、摄像机动画、设备模型：

| 任务 | 涉及文件 / 目录 |
|---|---|
| 重构 3D 场景布局（监管中枢 + 4 模块空间定位） | 改造 `src/simulation3d/builders/` |
| 监管智能体模型（发光圆柱 + 光环 torus） | 新建 `src/simulation3d/models/SupervisorModel.tsx` |
| 4 个专项 Agent 模型（发光球体 + 颜色区分） | 新建 `src/simulation3d/models/AgentSphere.tsx` |
| 管道系统（静态管道 + 粒子路径） | 改造 `src/simulation3d/builders/pipelineBuilder.ts` |
| 粒子飞行系统（红 / 橙 / 绿三色语义） | 改造 `src/simulation3d/builders/particleAnimBuilder.ts` |
| 摄像机聚焦 / 恢复动画 | 改造 `src/simulation3d/useCamera.ts` |
| Agent 思考气泡（3D HTML overlay） | 新建 `src/simulation3d/ThinkingBubble.tsx` |
| 设备动画（阀门旋转 / 泵转动 / 闪烁） | 新建 `src/simulation3d/animations/` |
| LOD 降级策略（FPS 检测 + 特效关闭） | 新建 `src/simulation3d/useLOD.ts` |
| `ScenarioPhase` 状态机驱动动画切换 | 新建 `src/simulation3d/useScenarioAnimator.ts` |

<!-- ⚠️ 以下为人员 A 新添加的任务建议（2026-05-28）
     我不清楚人员 B 当前的进度，以上原有任务可能已部分完成。
     以下是根据规划书和当前代码状态，补充的后续任务。
     请人员 B 自行评估优先级和排期。 -->

### 人员 B 后续补充任务

| # | 任务 | 涉及文件 / 目录 | 说明 |
|---|---|---|---|
| 1 | IDLE 态背景动画 | 新建 `src/simulation3d/effects/IdleAnimation.tsx` | 管道水流粒子、设备微动、偶发数据采集粒子 |
| 2 | 管道内水流效果 | 新建 `src/simulation3d/pipelines/WaterFlow.tsx` | shader UV 动画或内部粒子，IDLE 态持续运行 |
| 3 | 3D 场景环境氛围 | 新建 `src/simulation3d/environment/SceneEnvironment.tsx` | 反射地面、雾气、柔光圈、科技感天空盒 |
| 4 | 设备标签 HUD | 新建 `src/simulation3d/labels/DeviceLabel.tsx` | Html from drei，悬浮标签，异常变红 |
| 5 | 3D 设备点击交互 | 新建 `src/simulation3d/interactions/ClickHandler.tsx` | hover 发光 + 点击打开 Agent 窗口 |
| 6 | 设备动画细化 | 改造 `src/simulation3d/effects/DeviceAction.tsx` | 每种设备独立动画（阀门/泵/膜振动/温度发光） |
| 7 | LOD 降级实现 | 新建 `src/simulation3d/useLOD.ts` | 4 级 LOD，FPS 检测 + 逐级关闭特效 |
| 8 | 粒子系统性能优化 | 改造 `src/simulation3d/pipelines/ParticleSystem.tsx` | 确认 Points+BufferGeometry，粒子池化 |
| 9 | GLB 模型加载器 | 新建 `src/simulation3d/loaders/ModelLoader.tsx` | useGLTF 封装，配置化路径，fallback 几何体 |
| 10 | 模型↔动画解耦验证 | `src/simulation3d/equipment/` 各模块 | 确保动画作用于 group，模型可热替换 |

## 共享契约

在开始各自编码前，先一起定义这些接口文件：

| 文件 | 说明 |
|---|---|
| `src/stores/useScenarioStore.ts` | `ScenarioPhase` 枚举 + store 接口（A 写实现，B 只读消费） |
| `src/types/scenario.ts` | 共享类型定义（`ScenarioPhase`、`AgentId`、`AgentStatus` 等） |
| `src/stores/useSystemStore.ts` | 系统级指标接口（告警数 / 产水 / 健康度） |

**约定：**

- B 从 store 读取 `phase` 来驱动 3D 动画
- A 负责写入 `phase` 的状态转换逻辑
- 接口定好后，两人互不依赖

## 减少冲突的关键规则

- 不碰对方的目录：A 不改 `simulation3d/`，B 不改 `components/`
- `DashboardPage.tsx` 最后合并：各自开发完成后，由一个人统一把新组件组装进主页面
- 共享 store 接口先锁定：`types` 文件和 store 接口一旦定好，改动需要通知对方
- 各自建分支：A 用 `feat/os-desktop-ui`，B 用 `feat/3d-scene-engine`，最后合并到 `dev`