# Backend

Python backend entrypoint placeholder.

Backend code is intentionally not initialized yet. Put future backend source under `backend/app/`
and keep API contracts synchronized through `contracts/`.
